"use client";

import { Bell, Check, LockKeyhole, Save, ShieldCheck, UserRound } from "lucide-react";
import { useState } from "react";
import { AppShell } from "../../components/app-shell";
import { useAuth } from "../../components/auth-provider";

export default function Settings(){const {profile,updateProfile,configured}=useAuth();const [form,setForm]=useState(profile);const [tab,setTab]=useState("profile");const [saved,setSaved]=useState(false);async function save(){await updateProfile(form);setSaved(true);setTimeout(()=>setSaved(false),2500)}return <AppShell title="Profile & settings" eyebrow="Account preferences" actions={<button className="primary-button compact" onClick={save}><Save/>Save changes</button>}><div className="settings-layout"><aside className="settings-nav"><button className={tab==="profile"?"active":""} onClick={()=>setTab("profile")}><UserRound/>Profile</button><button className={tab==="guidance"?"active":""} onClick={()=>setTab("guidance")}><ShieldCheck/>Guidance</button><button className={tab==="privacy"?"active":""} onClick={()=>setTab("privacy")}><LockKeyhole/>Privacy</button><button className={tab==="notifications"?"active":""} onClick={()=>setTab("notifications")}><Bell/>Notifications</button></aside><section className="workspace-panel settings-card">{tab==="profile"&&<><div className="settings-heading"><UserRound/><div><h2>Personal profile</h2><p>Used to personalise your private workspace.</p></div></div><div className="settings-fields"><label><span>Full name</span><input value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label><label><span>Account role</span><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>Individual</option><option>Family representative</option><option>Student</option><option>Business owner</option><option>Mahallu / institution</option><option>Scholar / Qazi</option></select></label><label><span>Region</span><input value={form.region} onChange={e=>setForm({...form,region:e.target.value})}/></label><label><span>Language</span><select value={form.language} onChange={e=>setForm({...form,language:e.target.value})}><option>English</option><option>Malayalam</option><option>Arabic</option></select></label></div></>}{tab==="guidance"&&<><div className="settings-heading"><ShieldCheck/><div><h2>Guidance preferences</h2><p>This setting remains visible throughout calculations.</p></div></div><div className="madhhab-options">{["Hanafi","Maliki","Shafi'i","Hanbali"].map(m=><button key={m} className={form.madhhab===m?"active":""} onClick={()=>setForm({...form,madhhab:m})}><span>{m}</span>{form.madhhab===m&&<Check/>}</button>)}</div><div className="setting-info"><ShieldCheck/><p>Changing the madhhab does not silently rewrite saved reports. Existing reports keep their original method label.</p></div></>}{tab==="privacy"&&<><div className="settings-heading"><LockKeyhole/><div><h2>Privacy & data</h2><p>Control how private case information is stored and shared.</p></div></div><div className="privacy-cards"><article><strong>Database connection</strong><span>{configured?"Supabase connected":"Demo local storage"}</span></article><article><strong>Scholar sharing</strong><span>Consent required for every case</span></article><article><strong>Document links</strong><span>Temporary links recommended</span></article></div></>}{tab==="notifications"&&<><div className="settings-heading"><Bell/><div><h2>Notifications</h2><p>Choose which important account events reach you.</p></div></div><div className="notification-list">{["Appointment confirmations","Qazi review updates","Calculation reminders","Security alerts"].map(item=><label key={item}><input type="checkbox" defaultChecked/><span>{item}</span></label>)}</div></>}</section></div>{saved&&<div className="toast"><Check/>Profile settings saved</div>}</AppShell>}
